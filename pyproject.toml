[project]
name = "orbital-data-platform"
version = "0.1.0"
requires-python = ">=3.14.5"
dependencies = [
    "fastapi>=0.139.2",
    "uvicorn[standard]>=0.51.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.0",
]
